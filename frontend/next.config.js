/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Optional: If you plan to serve images from external domains
  // images: {
  //   domains: ['example.com', 'another-domain.com'],
  // },
  // Optional: For environment variables
  // env: {
  //   API_BASE_URL: process.env.API_BASE_URL,
  // },
  // Optional: i18n for internationalization
  // i18n: {
  //   locales: ['fa', 'en'],
  //   defaultLocale: 'fa',
  //   localeDetection: false, // Optional: disable auto-detection
  // },
};

module.exports = nextConfig;
