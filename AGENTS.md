# AGENTS.md - راهنمای توسعه برای دستیاران هوش مصنوعی

این فایل شامل دستورالعمل‌ها و نکاتی برای دستیاران هوش مصنوعی است که در توسعه پروژه FitClub مشارکت می‌کنند. لطفاً قبل از انجام هرگونه تغییر در کد، این موارد را مطالعه کنید.

## 🎯 هدف اصلی پروژه

هدف اصلی، ساخت یک پلتفرم کامل و کاربردی برای رزرو آنلاین باشگاه‌های ورزشی در ایران است، مشابه "جاباما" اما برای باشگاه‌ها. جزئیات کامل در صورت مسئله اولیه موجود است.

## 🛠️ تکنولوژی‌های اصلی

*   **Frontend:** React.js با Next.js و TailwindCSS
*   **Backend:** NestJS (بر پایه Node.js و Express)
*   **Database:** PostgreSQL
*   **Authentication:** JWT با Refresh Tokens
*   **زبان برنامه‌نویسی:** TypeScript برای هر دو بخش Frontend و Backend

## Структура پروژه

ساختار پوشه‌بندی پروژه به صورت زیر است:

```
fitclub/
├── frontend/  # Next.js application
├── backend/   # NestJS application
├── .gitignore
├── docker-compose.yml
├── Dockerfile.frontend
├── Dockerfile.backend
├── AGENTS.md  # This file
└── README.md
```

جزئیات بیشتر در مورد ساختار داخلی `frontend` و `backend` در پاسخ‌های اولیه ارائه شده است. لطفاً به آن ساختار پایبند باشید.

## 📝 اصول کدنویسی

1.  **خوانایی کد:** کد باید تمیز، خوانا و با کامنت‌های مناسب (در صورت نیاز) باشد.
2.  **ماژولار بودن:** تا حد امکان کدها را به صورت ماژولار بنویسید تا قابلیت استفاده مجدد و تست‌پذیری افزایش یابد.
3.  **TypeScript:** در هر دو بخش Frontend و Backend از TypeScript استفاده کنید و تا حد امکان از `any` پرهیز کنید. تایپ‌های دقیق تعریف کنید.
4.  **Next.js App Router:** در Frontend از App Router (معرفی شده در Next.js 13+) استفاده خواهد شد.
5.  **TailwindCSS:** برای استایل‌دهی در Frontend از TailwindCSS استفاده کنید. از ایجاد فایل‌های CSS جداگانه تا حد امکان خودداری کنید مگر اینکه برای کامپوننت‌های بسیار خاص و پیچیده لازم باشد.
6.  **NestJS Best Practices:** در Backend اصول و الگوهای پیشنهادی NestJS را رعایت کنید (Modules, Services, Controllers, DTOs, Guards, Pipes).
7.  **Error Handling:** مدیریت خطای مناسب در هر دو بخش Frontend و Backend ضروری است.
8.  **امنیت:** به موارد امنیتی ذکر شده در صورت مسئله (CSRF, XSS, SQL Injection, rate limiting) توجه ویژه داشته باشید.
9.  **تست‌نویسی:** برای منطق‌های اصلی و سرویس‌ها، تست‌های واحد (Unit Tests) و یکپارچه‌سازی (Integration Tests) بنویسید.
10. **کامی‌کردن تغییرات (Committing):** از پیام‌های کامیت واضح و معنادار استفاده کنید.

## 🔄 گردش کار (Workflow)

1.  **درک کامل تسک:** قبل از شروع کدنویسی، مطمئن شوید که تسک محول شده را به طور کامل درک کرده‌اید. در صورت وجود ابهام، سوال بپرسید.
2.  **برنامه‌ریزی:** برای تسک‌های پیچیده‌تر، یک برنامه کوچک برای خودتان در نظر بگیرید.
3.  **ایجاد شاخه (Branch):** برای هر فیچر یا باگ‌فیکس جدید، یک شاخه جداگانه از `main` یا `develop` ایجاد کنید. (نام‌گذاری پیشنهادی: `feature/feature-name` یا `fix/bug-description`)
4.  **توسعه و تست:** کد مورد نظر را پیاده‌سازی کرده و تست‌های لازم را بنویسید و اجرا کنید.
5.  **بازبینی و Refactor:** کد خود را بازبینی کنید و در صورت نیاز Refactor انجام دهید.
6.  **ارسال Pull Request:** پس از اتمام کار، یک Pull Request (PR) به شاخه اصلی ارسال کنید.

## 🌐 چندزبانگی (i18n)

از ابتدا ساختار پروژه باید قابلیت پشتیبانی از چند زبان (حداقل فارسی و انگلیسی) را داشته باشد. برای Frontend می‌توان از کتابخانه‌هایی مانند `next-intl` یا `react-i18next` استفاده کرد. رشته‌های متنی نباید به صورت hardcode در کد باشند.

## 🎨 Design System و UI/UX

*   به طراحی مدرن، مینیمال و کاربرپسند توجه شود.
*   واکنش‌گرایی (Mobile-first) بسیار مهم است.
*   پشتیبانی از حالت تیره (Dark Mode) باید در نظر گرفته شود.
*   کامپوننت‌های UI باید قابل استفاده مجدد و بخشی از یک Design System باشند.

## 🛑 موارد ممنوعه

*   استفاده از `any` به صورت بی‌رویه در TypeScript.
*   Hardcode کردن مقادیر حساس (مانند کلیدهای API) در کد. از متغیرهای محیطی (`.env` files) استفاده شود.
*   نادیده گرفتن اصول امنیتی.

## 💡 نکات دیگر

*   در صورت نیاز به نصب پکیج جدید، ابتدا بررسی کنید که آیا پکیج مشابهی قبلاً استفاده نشده است و سپس آن را به `package.json` اضافه کرده و فایل `package-lock.json` یا `yarn.lock` را آپدیت کنید.
*   همیشه به مستندات رسمی تکنولوژی‌های مورد استفاده مراجعه کنید.

این فایل به مرور زمان و با پیشرفت پروژه آپدیت خواهد شد.
موفق باشید!
